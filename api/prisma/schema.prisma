generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Costumer {
  id                 BigInt  @id
  phone              BigInt  @unique
  lastMessageTime    Int
  lastMessageContent String
  hasAnActiveChat    Boolean
}
